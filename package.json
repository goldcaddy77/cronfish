{
  "name": "cronfish",
  "version": "0.10.0",
  "description": "Drop a file. Cronfish does the rest. Markdown is a valid cron job.",
  "type": "module",
  "bin": {
    "cronfish": "./src/cli.ts"
  },
  "engines": {
    "bun": ">=1.0"
  },
  "scripts": {
    "test": "bun test",
    "build:ui": "cd ui && bun install && bun run build"
  },
  "files": [
    "src",
    "templates",
    "ui/dist",
    "README.md",
    "LICENSE"
  ],
  "license": "MIT",
  "author": "Dan Caddigan"
}
